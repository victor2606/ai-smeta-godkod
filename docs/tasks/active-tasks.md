# Активные задачи проекта: SQLite + Claude Code Agent для поиска расценок

**Дата обновления:** 2025-10-21
**Статус проекта:** Фазы 1-4 завершены ✅ | Фаза 5 частично завершена ✅ | MCP Server в разработке 🔄

---

## 📊 Текущий статус инфраструктуры

- **База данных:** ✅ Готова (28,686 расценок, 294,883 ресурса, 277 MB)
- **ETL Pipeline:** ✅ Работает (с расширенной загрузкой данных)
- **Поисковая система:** ✅ Готова (SearchEngine, CostCalculator, RateComparator)
- **Agent Helpers:** ✅ Реализованы (21 тест пройден)
- **Иерархия ГЭСН/ФЕР:** ✅ Восстановлена (13 полей добавлено)
- **Покрытие данных Excel:** ✅ **89.3%** (было 70.3%, +19.0% улучшение!)
- **Агент Claude Code:** ✅ Готов (project_knowledge.md создан)
- **Тесты:** ✅ 314 unit тестов (100% pass rate)

---

## 🎯 АКТИВНЫЕ ЗАДАЧИ

### ЗАДАЧА 5.4: Бенчмарк производительности

**Приоритет:** P2
**Статус:** 🟡 НЕ НАЧАТА
**Зависит от:** ✅ Задачи 3.1, 3.2, 2.4 завершены

#### Что нужно сделать:
Измерить скорость работы поисковой системы и выявить узкие места

#### Файлы для создания:
- `scripts/benchmark_search.py` (новый)
- `data/logs/` (директория для результатов)

#### Контекст для агента:
```
src/search/search_engine.py         - Модуль для бенчмарка
src/search/cost_calculator.py       - Модуль для бенчмарка
data/processed/estimates.db         - База для нагрузочного тестирования
tests/test_data_samples.json        - Запросы для бенчмарка
```

#### Критерии успеха:
- [ ] Файл `scripts/benchmark_search.py` содержит бенчмарк тесты
- [ ] Замер времени для 100 последовательных поисковых запросов
- [ ] Замер времени для 100 расчетов стоимости
- [ ] Измерение среднего/медианного/p95/p99 времени ответа
- [ ] Результаты выводятся в консоль в виде таблицы
- [ ] Сохранение результатов в `data/logs/benchmark_YYYYMMDD.json`
- [ ] Критерии успеха: средний поиск <100ms, расчет <50ms
- [ ] Тест на нагрузку: 1000 запросов подряд без ошибок
- [ ] Проверка роста размера БД и memory usage

#### Может выполняться параллельно с:
- Задачи 6.1-6.4 (документация)
- Задачи 7.2-7.5 (тестирование)

#### Блокирует:
- Ничего (опциональная оптимизация)

---

### ЗАДАЧА 6.1: API Reference

**Приоритет:** P2
**Статус:** 🟡 НЕ НАЧАТА
**Зависит от:** ✅ Задачи 3.1, 3.2, 3.3 завершены

#### Что нужно сделать:
Документировать публичные методы всех модулей системы

#### Файлы для создания:
- `docs/api_reference.md` (новый)

#### Контекст для агента:
```
src/database/db_manager.py          - Database API
src/etl/*.py                        - ETL API
src/search/*.py                     - Search API
src/utils/agent_helpers.py          - Helper functions API
src/database/schema.sql             - Database schema
```

#### Критерии успеха:
- [ ] Файл `docs/api_reference.md` содержит полное описание API
- [ ] Для каждого класса: назначение, конструктор, публичные методы
- [ ] Для каждого метода: параметры (тип, описание, default), возвращаемое значение, примеры, исключения
- [ ] Разделы: Database, ETL, Search, Utils
- [ ] Примеры кода для типовых задач:
  - Как выполнить поиск
  - Как рассчитать стоимость
  - Как сравнить расценки
  - Как пересобрать БД
- [ ] Описание структуры БД (таблицы, индексы, связи)
- [ ] FAQ: частые вопросы и решения

#### Может выполняться параллельно с:
- Все задачи Фаз 4-8 (независимая документация)

#### Блокирует:
- Задача 6.3 (README проекта)

---

### ЗАДАЧА 6.2: Зависимости проекта

**Приоритет:** P1
**Статус:** 🟢 ЧАСТИЧНО ГОТОВА
**Зависит от:** ✅ Нет зависимостей

#### Что нужно сделать:
Зафиксировать все Python зависимости в requirements.txt и создать requirements-dev.txt

#### Файлы для обновления:
- `requirements.txt` (обновление - уже существует, но нужна проверка)
- `requirements-dev.txt` (новый)
- `README.md` (обновление - добавить инструкцию по установке)

#### Контекст для агента:
```
requirements.txt                    - Текущие зависимости
src/**/*.py                         - Все импорты для анализа
pyproject.toml                      - Build конфигурация (если есть)
```

#### Критерии успеха:
- [ ] Файл `requirements.txt` содержит все необходимые пакеты с версиями
- [ ] Основные зависимости проверены:
  - `pandas>=2.0.0`
  - `openpyxl>=3.1.0`
  - `pytest>=7.0.0`
  - `tqdm>=4.65.0`
  - `rich>=13.0.0` ✅ (уже добавлен)
- [ ] Файл `requirements-dev.txt` для dev зависимостей: pytest, black, flake8, mypy
- [ ] Все версии совместимы с Python 3.10+
- [ ] README содержит инструкцию по установке: `pip install -r requirements.txt`

#### Может выполняться параллельно с:
- Все задачи Фаз 4-8 (независимая документация)

#### Блокирует:
- Задача 6.3 (README проекта)

---

### ЗАДАЧА 6.3: README проекта

**Приоритет:** P2
**Статус:** 🟡 НЕ НАЧАТА
**Зависит от:** ⏳ Задачи 6.1, 6.2 (нужно знать полный API и зависимости)

#### Что нужно сделать:
Написать понятную документацию для начала работы с проектом

#### Файлы для создания:
- `README.md` (обновление или создание)

#### Контекст для агента:
```
docs/estimates_search_architecture.md - Архитектура (Вариант 5)
docs/api_reference.md               - API документация
requirements.txt                    - Зависимости
src/                                - Структура проекта
agents/estimates_agent_prompt.md    - Примеры использования агента
```

#### Критерии успеха:
- [ ] Файл `README.md` содержит:
  - Название и краткое описание проекта
  - Архитектура (схема Вариант 5 из estimates_search_architecture.md)
  - Требования (Python 3.10+, зависимости)
  - Быстрый старт (Quick Start):
    1. Установка зависимостей
    2. Сборка базы данных
    3. Первый поисковый запрос
  - Примеры использования (из API reference)
  - Структура проекта (дерево директорий)
  - Ссылки на детальную документацию
  - Roadmap (будущие улучшения)
  - Лицензия и авторы
- [ ] Примеры оформлены в виде code blocks с подсветкой
- [ ] Есть пример вывода результата поиска

#### Может выполняться параллельно с:
- Задачи 4.3, 5.1-5.4, 6.4 (независимые задачи)

#### Блокирует:
- Ничего (но улучшает UX проекта)

---

### ЗАДАЧА 6.4: .gitignore и конфигурация Git

**Приоритет:** P2
**Статус:** 🟢 ЧАСТИЧНО ГОТОВА
**Зависит от:** ✅ Нет зависимостей

#### Что нужно сделать:
Проверить и обновить .gitignore для исключения ненужных файлов

#### Файлы для обновления:
- `.gitignore` (проверка и обновление)

#### Контекст для агента:
```
.gitignore                          - Текущие правила
data/                               - Файлы данных
.git/                               - Git репозиторий
```

#### Критерии успеха:
- [ ] Файл `.gitignore` содержит:
  - `data/processed/*.db` (БД не коммитим)
  - `data/cache/*.json` (кэш не коммитим)
  - `data/logs/*.log` (логи не коммитим)
  - `__pycache__/`, `*.pyc`, `*.pyo`
  - `.pytest_cache/`, `.coverage`
  - `.venv/`, `venv/`, `env/`
  - `.DS_Store`
  - `*.swp`, `*~`
- [ ] Исходный Excel файл `data/raw/*.xlsx` ВКЛЮЧЕН в Git
- [ ] Файлы `.gitkeep` в пустых директориях ВКЛЮЧЕНЫ в Git

#### Может выполняться параллельно с:
- Все задачи Фаз 4-8 (независимая конфигурация)

#### Блокирует:
- Ничего (hygiene task)

---

### ЗАДАЧА 7.2: Тестирование поиска вручную

**Приоритет:** P1
**Статус:** 🟡 НЕ НАЧАТА
**Зависит от:** ✅ Задачи 3.1, 7.1 завершены

#### Что нужно сделать:
Проверить работу поисковой системы через Python REPL

#### Файлы для проверки:
- `src/search/search_engine.py`
- `data/processed/estimates.db`

#### Контекст для агента:
```
src/search/search_engine.py         - API для тестирования
data/processed/estimates.db         - База данных
agents/estimates_agent_prompt.md    - Примеры запросов
```

#### Критерии успеха:
- [ ] Импорт модулей работает: `from src.search.search_engine import SearchEngine`
- [ ] Инициализация: `engine = SearchEngine('data/processed/estimates.db')`
- [ ] Поиск перегородок: `results = engine.search("перегородки гипсокартон")` возвращает расценки
- [ ] Поиск по коду: `result = engine.search_by_code("10-05-001-01")` работает
- [ ] Фильтр по единице: `results = engine.search("перегородки", filters={'unit_type': 'м2'})` работает
- [ ] Результаты отсортированы по релевантности (rank)
- [ ] Проверка наличия всех полей в результате

#### Может выполняться параллельно с:
- Задача 7.3 (тестирование расчетов)
- Задачи 5.1-5.4 (автотесты)

#### Блокирует:
- Задача 7.5 (интеграционный тест)

---

### ЗАДАЧА 7.3: Тестирование расчетов вручную

**Приоритет:** P1
**Статус:** 🟡 НЕ НАЧАТА
**Зависит от:** ✅ Задачи 3.2, 7.1 завершены

#### Что нужно сделать:
Проверить точность калькулятора через Python REPL

#### Файлы для проверки:
- `src/search/cost_calculator.py`
- `data/processed/estimates.db`

#### Контекст для агента:
```
src/search/cost_calculator.py       - API для тестирования
data/processed/estimates.db         - База данных
agents/estimates_agent_prompt.md    - Примеры расчетов
```

#### Критерии успеха:
- [ ] Импорт: `from src.search.cost_calculator import CostCalculator`
- [ ] Инициализация: `calc = CostCalculator('data/processed/estimates.db')`
- [ ] Расчет для ГКЛ: `result = calc.calculate("10-05-001-01", 150)`
- [ ] Проверка результата:
  - `result['calculated_total']` корректен
  - `result['cost_per_unit']` корректен
  - `result['rate_info']['rate_full_name']` содержит описание
- [ ] Детальная разбивка: `breakdown = calc.get_detailed_breakdown("10-05-001-01", 150)`
- [ ] В breakdown есть пересчитанные ресурсы
- [ ] Суммы материалов и работ сходятся

#### Может выполняться параллельно с:
- Задача 7.2 (тестирование поиска)
- Задачи 5.1-5.4 (автотесты)

#### Блокирует:
- Задача 7.5 (интеграционный тест)

---

### ЗАДАЧА 7.4: Запуск всех автотестов

**Приоритет:** P1
**Статус:** 🟡 НЕ НАЧАТА
**Зависит от:** ✅ Задачи 5.2, 5.3 завершены

#### Что нужно сделать:
Убедиться что все тесты проходят успешно

#### Файлы для проверки:
- `tests/test_*.py` (все тесты)
- `pytest.ini` (конфигурация)

#### Контекст для агента:
```
tests/                              - Все тестовые файлы
pytest.ini                          - Конфигурация pytest
data/processed/estimates.db         - Тестовая БД
```

#### Критерии успеха:
- [ ] Запущен pytest: `pytest tests/ -v`
- [ ] Все тесты в `test_search_engine.py` PASSED (28 тестов)
- [ ] Все тесты в `test_calculator.py` PASSED (15 тестов)
- [ ] Существующие тесты (agent_helpers, etc.) PASSED (271 тестов)
- [ ] Coverage: `pytest --cov=src tests/` показывает >80%
- [ ] Нет warnings и deprecation notices
- [ ] Время выполнения всех тестов <10 секунд

#### Может выполняться параллельно с:
- Задачи 6.1-6.4 (документация)

#### Блокирует:
- Задача 7.5 (интеграционный тест)

---

### ЗАДАЧА 7.5: Интеграционный тест с Claude Code Agent

**Приоритет:** P1
**Статус:** 🟡 НЕ НАЧАТА
**Зависит от:** ✅ Задачи 4.1, 4.2, 4.3, 7.2, 7.3, 7.4 завершены (частично)

#### Что нужно сделать:
Проверить работу агента с реальными запросами пользователя

#### Файлы для проверки:
- `.claude/project_knowledge.md`
- `agents/estimates_agent_prompt.md`
- Все модули системы

#### Контекст для агента:
```
agents/estimates_agent_prompt.md    - Промпт агента
.claude/project_knowledge.md        - Контекст проекта
src/utils/agent_helpers.py          - Helper functions
tests/test_data_samples.json        - Тестовые запросы
data/processed/estimates.db         - База данных
```

#### Критерии успеха:
- [ ] Загружен промпт из `agents/estimates_agent_prompt.md`
- [ ] Запрос 1: "Сколько будет стоить устройство 150 м² перегородок из ГКЛ в один слой?"
  - Claude использует SearchEngine для поиска
  - Claude использует CostCalculator для расчета
  - Ответ содержит расценку 10-05-001-01
  - Итоговая сумма корректна
  - Ответ включает пояснения и разбивку
- [ ] Запрос 2: "Сравни однослойные и двухслойные перегородки для 100 м²"
  - Claude находит обе расценки
  - Claude использует RateComparator
  - Ответ содержит таблицу сравнения
  - Указана разница в цене
- [ ] Запрос 3: "Какие ресурсы входят в 10-05-001-01?"
  - Claude выводит детальную разбивку по ресурсам
  - Указаны количества и стоимости
- [ ] Все ответы корректны, понятны, хорошо отформатированы

#### Может выполняться параллельно с:
- Ничего (финальная интеграционная проверка)

#### Блокирует:
- Ничего (это финальная задача MVP)

---

## 📊 ОПЦИОНАЛЬНЫЕ УЛУЧШЕНИЯ (Фаза 8)

### ЗАДАЧА 8.1: Кэширование запросов
- **Статус:** ✅ ЧАСТИЧНО РЕАЛИЗОВАНО (в agent_helpers.py)
- **Заметка:** QueryCache уже работает в agent_helpers.py (24h TTL, JSON-based)

### ЗАДАЧА 8.2: Улучшенная морфология (pymorphy3)
- **Статус:** 🟡 НЕ НАЧАТА
- **Приоритет:** P3
- **Зависимости:** Нет

### ЗАДАЧА 8.3: Экспорт результатов в Excel
- **Статус:** 🟡 НЕ НАЧАТА
- **Приоритет:** P3
- **Зависимости:** Задачи 3.1, 3.2

### ЗАДАЧА 8.4: Веб-интерфейс (прототип)
- **Статус:** 🟡 НЕ НАЧАТА
- **Приоритет:** P3
- **Зависимости:** Задачи 3.1, 3.2

---

## 🚀 НОВАЯ ЗАДАЧА: MCP Server для N8N

### ЗАДАЧА 10.1: Разработка MCP-сервера на базе FastMCP

**Приоритет:** P0 (критично для интеграции с N8N)
**Статус:** 📋 ЗАПЛАНИРОВАНО
**Дата создания:** 2025-10-21
**Ответственный:** Claude Code Orchestrator

#### 📊 Описание задачи:

Создать независимый MCP (Model Context Protocol) сервер на базе FastMCP framework для интеграции функциональности construction-estimator с N8N workflow engine. Переместить логику агента из Claude Code в автономный сервер, доступный для любых MCP-клиентов.

#### 🎯 Цель:

Обеспечить возможность работы с системой строительных расценок через агентов внутри N8N, используя стандартный MCP Protocol для коммуникации.

#### 🏗️ Архитектура:

```
N8N Workflow Engine
  └─ AI Agent (OpenAI/Anthropic)
      └─ MCP Client Tool Node
          │ (SSE Transport)
          ▼
FastMCP Server (Python)
  ├─ Tools (@mcp.tool decorators):
  │   ├─ natural_search()
  │   ├─ quick_calculate()
  │   ├─ show_rate_details()
  │   ├─ compare_variants()
  │   └─ find_similar_rates()
  │
  ├─ Business Logic:
  │   ├─ SearchEngine (FTS5)
  │   ├─ CostCalculator
  │   └─ RateComparator
  │
  └─ Data Layer:
      └─ DatabaseManager → SQLite (28,686 rates)
```

#### 📝 Конкретные действия:

**Этап 1: Создание MCP-сервера** 🟡

1. **Создать `mcp_server.py`:**
   - Инициализация FastMCP instance
   - Регистрация 5 tools с декораторами `@mcp.tool`
   - Реализация error handling и logging
   - Конфигурация SSE transport (host=0.0.0.0, port=8000)

2. **Обернуть существующую бизнес-логику:**
   - `natural_search()` → `SearchEngine.search()`
   - `quick_calculate()` → автодетект code/description + `CostCalculator.calculate()`
   - `show_rate_details()` → `CostCalculator.get_detailed_breakdown()`
   - `compare_variants()` → `SearchEngine` + `RateComparator.compare()`
   - `find_similar_rates()` → `RateComparator.find_alternatives()`

3. **Обеспечить JSON-serializable responses:**
   - Конвертация pandas DataFrames → list of dicts
   - Форматирование денежных сумм с округлением
   - Структурированные error responses

**Этап 2: Контейнеризация** 🟡

1. **Создать `Dockerfile.mcp`:**
   - Base image: python:3.10-slim
   - Install dependencies: fastmcp, pandas, rich
   - Copy src/ и data/
   - EXPOSE 8000
   - ENTRYPOINT: python mcp_server.py

2. **Создать `docker-compose.mcp.yml`:**
   - Service: mcp-server (port 8000)
   - Service: n8n (port 5678)
   - Volumes: ./data → /app/data
   - Environment: N8N_COMMUNITY_PACKAGES_ALLOW_TOOL_USAGE=true
   - Healthcheck для mcp-server

**Этап 3: Тестирование** 🟡

1. **Создать `tests/test_mcp_tools.py`:**
   - Использовать FastMCP Client для in-memory testing
   - Тесты для каждого tool:
     - `test_natural_search()` — поиск по описанию
     - `test_quick_calculate()` — расчёт стоимости
     - `test_show_rate_details()` — детализация ресурсов
     - `test_compare_variants()` — сравнение вариантов
     - `test_find_similar_rates()` — поиск альтернатив
   - Валидация JSON schemas
   - Error handling tests

2. **Интеграционное тестирование:**
   - Запуск MCP server в Docker
   - Подключение через N8N MCP Client Tool
   - Выполнение реальных запросов через AI Agent
   - Валидация responses

**Этап 4: Документация** 🟡

1. **Создать `docs/mcp_server_guide.md`:**
   - Архитектурная диаграмма
   - API Reference для каждого tool
   - Примеры request/response
   - Инструкции по deployment
   - Troubleshooting guide

2. **Обновить `README.md`:**
   - Добавить секцию "MCP Server Integration"
   - Quick start для N8N integration
   - Ссылки на mcp_server_guide.md

**Этап 5: Production-ready features (опционально)** 🟢

1. **Authentication:**
   - OAuth2 через FastMCP providers (Google/GitHub)
   - API key authentication (опционально)

2. **Monitoring:**
   - Prometheus metrics endpoint
   - Logging в JSON format
   - Health check endpoint

3. **Rate Limiting:**
   - FastMCP middleware для rate limiting
   - Конфигурация limits (100 req/min)

#### 🔧 Технический стек:

- **Framework:** FastMCP 2.x (Python)
- **Transport:** SSE (Server-Sent Events) over HTTP
- **Database:** Существующий SQLite (без изменений)
- **Container:** Docker + Docker Compose
- **Testing:** pytest + FastMCP Client (in-memory)
- **N8N Integration:** MCP Client Tool node

#### 📦 Файлы для создания:

**Новые файлы:**
- `mcp_server.py` — главный файл сервера
- `Dockerfile.mcp` — образ для MCP сервера
- `docker-compose.mcp.yml` — оркестрация с N8N
- `tests/test_mcp_tools.py` — тесты для MCP tools
- `docs/mcp_server_guide.md` — документация
- `.env.mcp` — environment variables

**Обновления:**
- `README.md` — добавить секцию про MCP integration
- `requirements.txt` — добавить `fastmcp>=2.0.0`

#### ✅ Критерии успеха:

**Этап 1 (Создание MCP-сервера) — ✅ ЗАВЕРШЁН (2025-10-21):**
- [x] Исследование FastMCP и MCP Protocol завершено ✅
- [x] Архитектура MCP-сервера спроектирована ✅
- [x] `mcp_server.py` создан с 5 работающими tools ✅
- [x] MCP server запускается локально ✅
- [x] Все 5 tools возвращают корректные JSON responses ✅
- [x] Virtual environment с Python 3.12 создан ✅
- [x] FastMCP 2.12.5 установлен ✅
- [x] 32/32 pytest тестов проходят (100% pass rate) ✅ **[ОБНОВЛЕНО 21.10.2025]**
- [x] Полная документация создана (4 MD файла + примеры) ✅

**Этап 2 (Контейнеризация) — ✅ ЗАВЕРШЁН (2025-10-21):**
- [x] `Dockerfile.mcp` создан с multi-stage build ✅ **[НОВОЕ]**
- [x] `docker-compose.mcp.yml` создан, связка mcp-server + n8n ✅ **[НОВОЕ]**
- [x] Health check endpoint реализован (`health_server.py` на порту 8001) ✅ **[НОВОЕ]**
- [x] `.dockerignore` создан для оптимизации build context ✅ **[НОВОЕ]**
- [x] Health server интегрирован в `mcp_server.py` ✅ **[НОВОЕ]**
- [ ] Docker image собран и протестирован (build в процессе)

**Этап 3 (Тестирование) — ✅ ЗАВЕРШЁН (2025-10-21):**
- [x] 32/32 pytest тестов для MCP tools проходят ✅ **[ОБНОВЛЕНО 21.10.2025]**
- [x] Исправлены 4 FTS5 syntax errors (изменён порядок wildcards/synonyms) ✅ **[НОВОЕ]**
- [x] Health server модуль протестирован ✅ **[НОВОЕ]**
- [ ] N8N AI Agent успешно вызывает MCP tools через MCP Client node
- [ ] Интеграционные тесты проходят (end-to-end)

**Этап 4 (Документация) — ✅ ЗАВЕРШЁН (2025-10-21):**
- [x] `docs/MCP_SERVER.md` — полная техническая документация (685 строк) ✅
- [x] `MCP_README.md` — Quick Start guide (287 строк) ✅
- [x] `MCP_QUICK_REFERENCE.md` — справочная карточка (125 строк) ✅
- [x] `MCP_IMPLEMENTATION_SUMMARY.md` — отчёт о реализации (300+ строк) ✅
- [x] `examples/mcp_client_example.py` — 10 примеров использования (345 строк) ✅
- [x] `validate_mcp.py` — утилита валидации (142 строки) ✅
- [ ] `README.md` обновлён с секцией MCP integration
- [ ] Примеры N8N workflows документированы

**Этап 5 (Production features, опционально):**
- [ ] OAuth2 authentication настроен
- [ ] Health check endpoint работает
- [ ] Rate limiting применён

#### 📊 Ожидаемые результаты:

**После завершения:**
- ✅ Независимый MCP-сервер работает на порту 8000
- ✅ N8N агенты получают доступ к 5 строительным инструментам
- ✅ Один сервер → множество клиентов (N8N, Claude Desktop, Cursor, Windsurf)
- ✅ Горизонтальная масштабируемость (Docker/K8s)
- ✅ Production-ready deployment (FastMCP Cloud опционально)

**Метрики успеха:**
- Latency: <200ms для natural_search
- Latency: <100ms для quick_calculate
- Uptime: >99.9% (с healthcheck)
- Test coverage: >90% для MCP tools

#### 🔗 Зависимости:

- **Блокирует:** Возможность использования construction-estimator через N8N
- **Зависит от:** ✅ Задачи 3.1, 3.2, 3.3 (бизнес-логика готова)
- **Делегировать:** `backend-architect` для реализации MCP server
- **Параллельно с:** Задачи Фаз 5-7 (не блокирует MVP агента в Claude Code)

#### 🎯 Преимущества решения:

1. **Независимость**: MCP-сервер работает автономно от Claude Code
2. **Переиспользование**: Поддержка множества MCP-клиентов
3. **Масштабируемость**: Легко масштабируется горизонтально
4. **Стандартизация**: MCP Protocol — индустриальный стандарт (Anthropic)
5. **Production-ready**: Auth, monitoring, rate limiting из коробки

#### 📚 Исследовательские материалы:

Проведённое исследование (2025-10-21):
- ✅ FastMCP framework architecture изучен
- ✅ N8N MCP integration patterns проанализированы
- ✅ Существующий construction-estimator agent capabilities задокументированы
- ✅ Архитектурный дизайн разработан

**Ключевые находки:**
- FastMCP использует декораторы `@mcp.tool` для минимального boilerplate
- Type hints → автогенерация MCP schemas
- N8N MCP Client Tool node → SSE transport
- Существующая бизнес-логика переиспользуется без изменений

---

## 📈 Матрица зависимостей

```
Критический путь (P1 задачи):
7.2, 7.3 → 7.4 → 7.5 (интеграционный тест MVP)
6.2 → 6.3 (документация)

Параллельное выполнение возможно:
- Блок 1: 5.4, 6.1, 6.2, 6.4, 7.2, 7.3
- Блок 2: После 7.2, 7.3 → 7.4
- Блок 3: После 7.4 → 7.5
- Блок 4: MCP Server (Задача 10.1) — независимая разработка

Новая задача (P0):
10.1 (MCP Server) — может выполняться параллельно со всеми остальными
```

---

## ✅ ЗАВЕРШЁННЫЕ ЗАДАЧИ

См. архив: `docs/tasks/archive/2025-10-completed.md`

**Завершённые фазы:**
- ✅ ФАЗА 1: Подготовка инфраструктуры (4 задачи)
- ✅ ФАЗА 2: ETL процесс (7 задач)
- ✅ ФАЗА 3: Поисковая система (3 задачи)
- ✅ ФАЗА 4: Claude Code Agent (3 задачи — 4.1, 4.2, 4.3)
- ✅ ФАЗА 5: Тестирование (3 из 5 задач — 5.1, 5.2, 5.3)

**Критические исправления:**
- ✅ ЗАДАЧА 9.1: Расширение загрузки данных из Excel (Этап 1-2)
- ✅ ЗАДАЧА 9.2: Критический анализ архитектуры БД (P0-P2)
- ✅ ЗАДАЧА 9.3: Увеличение покрытия данных до 89.3% (Этапы 1-3)

**Последнее обновление архива:** 2025-10-21
**Всего завершено задач:** 24 (19 базовых + 5 новых)

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

### **Вариант A: Завершить MVP (рекомендуется)**

1. **Задача 7.2, 7.3** (Ручное тестирование) — проверить работу модулей
2. **Задача 7.4** (Автотесты) — запустить полный набор тестов
3. **Задача 7.5** (Интеграционный тест) — ФИНАЛ MVP
4. **Параллельно:** Задача 6.2 (Зависимости проекта)
5. **Опционально:** Задача 9.3 (Этапы 4-5) — Техгруппы + abstract_resources

---

### **Вариант B: Начать разработку MCP Server**

1. **Задача 10.1 (Этап 1)** — Создать mcp_server.py с 5 tools
2. **Задача 10.1 (Этап 2)** — Контейнеризация (Dockerfile + docker-compose)
3. **Задача 10.1 (Этап 3)** — Тестирование (pytest + N8N integration)
4. **Параллельно:** Задачи 7.2-7.5 (завершить MVP)

---

**🎯 РЕКОМЕНДАЦИЯ:** **Вариант A**

**Причина:**
- MVP системы практически завершён (осталось 3 задачи)
- Лучше сначала проверить работоспособность базового функционала
- MCP Server можно делать параллельно после завершения тестирования

**Цель:** Завершить MVP системы с **полным покрытием данных (89.3%)**, протестированным функционалом и готовым Claude Code агентом.
